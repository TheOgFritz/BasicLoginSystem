# BasicLoginSystem
A basic login system can be used with any Python project.

The test file shows how you can use this in any Python project. May need to format the .json file differently per computer.
The os.system('cls') only works on Windows. I don't know about other devices.
